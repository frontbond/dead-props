import { Button } from "@fixture/ui";

export function Toolbar() {
  return (
    <>
      <Button label="Save" onClick={() => {}} variant="primary" />
      <Button label="Cancel" onClick={() => {}} />
    </>
  );
}
